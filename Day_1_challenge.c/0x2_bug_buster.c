#include <stdio.h>

/**
*main - Entry point of our code
*Description: A program that fix the bugs in the code
*Return: Always(success)
*/

/* In the initial code, semi-colon was added to the main function*/
/* Which resulted to error*/

int main(void)
{
	printf("Keep looking!");
	printf("You\'ll find it!\n");
	return (0);
}
